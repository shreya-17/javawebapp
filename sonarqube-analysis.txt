node {
  stage ('Build') {

    git url: 'https://github.com/shreya-17/Pixlmarketo.git'

    node {
       "mvn clean install"
         }
        maven: 'maven-3'
      {

      // Run the maven build
     mvn sonar:sonar 
  
      } 
  }
}
